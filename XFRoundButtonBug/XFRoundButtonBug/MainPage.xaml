<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XFRoundButtonBug"
             x:Class="XFRoundButtonBug.MainPage">

    <StackLayout Margin="15">
        <!-- Place new controls here -->
        <Label 
            Margin="5"
            Text="Button Hardcoded to 5"></Label>
        <Button 
            Margin="5"
            WidthRequest="50"
            HeightRequest="50"
            CornerRadius="5"
            ></Button>
        <Entry Text="{Binding TheButtonCornerRadius}"></Entry>
        <Label Text="The corner radius here is bound to the value entered above. Setting it to '5', the same as the hardcoded value above, will cause it to crash. Setting it to either 4 or 6 will not crash. "></Label>
        <Button 
            Margin="5"
            WidthRequest="50"
            HeightRequest="50"
            CornerRadius="{Binding TheButtonCornerRadius}"
            ></Button>
    </StackLayout>

</ContentPage>
